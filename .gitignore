# ── Secrets ──────────────────────────────
.env
*.env
!.env.example

# ── Private workflow instructions ───────
profiles/private/*.txt

# ── Local SQLite (legacy/backup) ────────
data/.credential_key
**/.credential_key
data/*.db
data/*.db-*
data/uploads/
data/outputs/

# ── Firebase ─────────────────────────────
firebase-adminsdk*.json
serviceAccountKey.json
*firebase*credentials*.json
.firebaserc

# ── Turso / libSQL ───────────────────────
*.db-backup
*_backup.db
data/image_agent_before_*.db
.turso/
backup_before_turso/
*.sql
*_dump.sql

# ── Cloudflare (R2/Queues/Worker secrets) ──
.wrangler/
wrangler.toml
.dev.vars
cloudflare-worker/.dev.vars
cloudflare-worker/wrangler.toml
cloudflare-worker/*secret*

# ── Python environments ─────────────────
.venv/
.venv_python314_backup/
venv/
env/
__pycache__/
*.py[cod]

# ── Editor / OS ──────────────────────────
.vscode/
.DS_Store
Thumbs.db

# ── Node (if any frontend tooling added later) ──
node_modules/