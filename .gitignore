# ── Secrets ──────────────────────────────
.env
*.env
!.env.example

# ── Private workflow instructions ───────
profiles/private/*.txt

# ── Local SQLite (legacy/backup) ────────
data/.credential_key
data/*.db
data/*.db-*
data/uploads/
data/outputs/

# ── Firebase ─────────────────────────────
firebase-adminsdk*.json
serviceAccountKey.json
*firebase*credentials*.json
.firebaserc

# ── Turso / libSQL ───────────────────────
*.db-backup
*_backup.db
data/image_agent_before_*.db
.turso/

# ── Cloudflare (future R2/Queues) ───────
.wrangler/
wrangler.toml
.dev.vars

# ── Python ───────────────────────────────
.venv/
venv/
env/
__pycache__/
*.py[cod]

# ── Editor / OS ──────────────────────────
.vscode/
.DS_Store
Thumbs.db

# ── Node (if any frontend tooling added later) ──
node_modules/